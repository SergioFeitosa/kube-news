pipeline {
    agent any

    stages {

        stage('Build Docker Image Test') {
            steps {
                script{
                    dockerapp = docker.build("sergiofeitosa/kube-news:${env.BUILD_ID}", '-f ./scr/Dockerfile .')
                }
            }
        }
    }
}